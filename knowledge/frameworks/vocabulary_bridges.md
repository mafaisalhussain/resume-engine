# Vocabulary Bridges

| JD term | Candidate's actual work | How to write it |
|---|---|---|
| "agentic workflow" / "AI agent" | Multi-model orchestration (Mistral, LLaMA3) with session routing | "agentic multi-model pipeline" or "LLM agent architecture" |
| "semantic search" | ChromaDB similarity retrieval with MiniLM embeddings | "semantic vector search via ChromaDB" |
| "vector similarity retrieval" | ChromaDB + sentence-transformer indexing | "vector similarity retrieval using ChromaDB + MiniLM embeddings" |
| "model serving" / "inference serving" | FastAPI + Docker + Kubernetes | "model serving via FastAPI on Kubernetes" |
| "document intelligence" | RAG pipeline ingesting legal PDFs/web sources | "document intelligence pipeline via RAG" |
| "LLM evaluation" / "response quality" | 9 NLP techniques: extractive QA, NER, topic modeling, sentiment | "LLM response evaluation via extractive QA (RoBERTa-SQuAD2), NER, topic modeling" |
| "data pipeline orchestration" | PySpark ETL, Pandas preprocessing | "data pipeline orchestration using PySpark / Pandas" |
| "anomaly detection" | Isolation Forest on live Scapy packet streams | "real-time anomaly detection using Isolation Forest" |
| "interpretable ML" / "explainability" | SHAP TreeExplainer on LightGBM | "model interpretability via SHAP TreeExplainer" |
| "REST API design" | FastAPI backend with streaming NDJSON inference | "REST API design using FastAPI with streaming inference" |
| "feature engineering" | 35 financial features; packet-level statistical features | "feature engineering from [source] yielding [N] features" |
